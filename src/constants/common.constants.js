import { ENV_CONFIG } from "../config/env.config.js";

// HTTP Status Codes
const HTTP_STATUS = {
  OK: 200,
  CREATED: 201,
  BAD_REQUEST: 400,
  UNAUTHORIZED: 401,
  FORBIDDEN: 403,
  NOT_FOUND: 404,
  INTERNAL_SERVER_ERROR: 500,
  TOO_MANY_REQUESTS: 429,
  CONFLICT: 409,
};

// JWT Configuration
const JWT_CONFIG = {
  SECRET: ENV_CONFIG.JWT_SECRET,
  EXPIRES_IN: ENV_CONFIG.JWT_EXPIRES_IN,
  ACCESS_TOKEN_EXPIRES_IN: ENV_CONFIG.JWT_ACCESS_EXPIRES_IN,
  REFRESH_TOKEN_EXPIRES_IN: ENV_CONFIG.JWT_REFRESH_EXPIRES_IN,
};

//TWILIO Configuration
const TWILIO_CONFIG = {
  ACCOUT_SID: ENV_CONFIG.TWILIO_ACCOUNT_SID,
  AUTH_TOKEN: ENV_CONFIG.TWILIO_AUTH_TOKEN
}

// AWS S3 Configuration
const S3_CONFIG = {
  ACCESS_KEY_ID: ENV_CONFIG.AWS_ACCESS_KEY_ID,
  SECRET_ACCESS_KEY: ENV_CONFIG.AWS_SECRET_ACCESS_KEY,
  REGION: ENV_CONFIG.AWS_REGION,
  BUCKET_NAME: ENV_CONFIG.S3_BUCKET_NAME
}

// Database Configuration
const DB_CONFIG = {
  URI: ENV_CONFIG.MONGODB_URI,
};


const SERVER_MESSAGES = {
  ERROR: {
    INTERNAL_SERVER_ERROR: 'Internal server error',
    VALIDATION_ERROR: 'Validation error',
    UNAUTHORIZED: 'Unauthorized access',
    FORBIDDEN: 'Forbidden access',
    NOT_FOUND: 'Resource not found',
    BAD_REQUEST: 'Bad request',
  }
};

export const MAX_FILE_SIZE = 1 * 1024 * 1024; // 10MB


export {
  HTTP_STATUS,
  JWT_CONFIG,
  DB_CONFIG,
  SERVER_MESSAGES,
  TWILIO_CONFIG,
  S3_CONFIG
};
